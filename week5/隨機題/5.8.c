/*進制轉換，二/八/十進制之間做轉換。
輸入第一個數字為輸入格式，以下N個數字為輸入數字由左到右的數字
輸入輸出皆為一行代表一個位數
最後一個數字是輸出格式。*/

#include<stdio.h>

main(){
    int it,number,ot;//輸入，位數
    scanf("%d%d",&it,&number);

    int value;
    int a;
    for(a=0;a<number;a++){
        scanf("%d",&)
    }

    if(it==2){
        if(ot==8){

        }
        else if(ot==10){

        }
    }
    else if(it==8){
        if(ot==2){

        }
        else if(ot==10){

        }
    }
    else if(it==10){
        if(ot==2){

        }
        else if(ot==8){

        }
    }
}
